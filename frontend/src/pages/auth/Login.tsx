import { LoginForm } from '../../components/LoginForm';

const Login = () => {
  return (
    <div className="min-h-screen bg-gradient-to-br from-slate-50 via-blue-50 to-cyan-100 px-6 py-8 grid place-items-center">
      <LoginForm />
    </div>
  );
};

export default Login;
